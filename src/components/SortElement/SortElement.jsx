import React from "react";

export default function SortElement(props) {
  return (
    <div
      style={{
        height: props.height,
        backgroundColor: "lightgreen",
        width: props.width
      }}
    ></div>
  );
}
